{
  "name": "signal-developer-mode",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "got": "^11.7.0"
  },
  "devDependencies": {
    "twilio-run": "^3.3.0"
  },
  "engines": {
    "node": "10"
  }
}
